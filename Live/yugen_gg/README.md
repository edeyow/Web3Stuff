# @yugen_gg — Wallet Collection Bypass

Japanese-themed NFT project. Site has a hidden "ritual" puzzle (fire → wind → water → void → earth) that reveals a wallet form. Submission goes to a Supabase Edge Function with zero verification.

**Bypass:** Direct API call — no puzzle, no browser needed.

## Usage
1. Open `https://yugen.gg`
2. F12 → Console → paste `register.js` → Enter
3. Panel appears — paste entries (`@handle,0xwallet` per line)
4. Click Submit

No social task verification, no game completion check.
